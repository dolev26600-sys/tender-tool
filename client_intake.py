#!/usr/bin/env python3
"""
שיחת אפיון שהלקוח מנהל בעצמו, בלי היועץ.

הרעיון: פגישת האפיון היא עלות קבועה של שעה-שעתיים לכל לקוח, והיא מורכבת
ברובה משאלות קבועות שאפשר לשאול בלי שיקול דעת מקצועי. הכלי הזה מנהל את
החלק הזה מול הלקוח - היועץ נכנס רק אחר כך, לתיק מלא ומסודר.

מה זה **לא** עושה, בכוונה:
- לא נותן ייעוץ, לא מציע תמהיל, לא מזכיר ריביות ולא מבטיח שום דבר.
- לא מחליף את היועץ. הוא אוסף את מה שהיועץ היה שואל ממילא, וכל שאלה
  מקצועית שהלקוח מעלה מנותבת חזרה ליועץ.

בסוף השיחה, התמלול עובר דרך meeting_intake.process_meeting - אותו מנוע
שמעבד הערות מפגישה רגילה - כך שהפלט זהה: רשומה מובנית, רשימת מסמכים,
ומה שהלקוח אמר על העתיד.
"""
from __future__ import annotations

from typing import Optional

import anthropic

MODEL = "claude-opus-4-8"

# מה שצריך לאסוף. מוצג למודל כרשימה, לא כטופס - השיחה צריכה להישמע
# כמו שיחה, לא כמו חקירה.
INTAKE_CHECKLIST = """- מי הלווים: שמות, גילאים
- תעסוקה של כל אחד: שכיר או עצמאי, איפה, כמה זמן
- הכנסה חודשית נטו של כל אחד
- הלוואות והתחייבויות קיימות (רכב, אשראי, הלוואות אחרות)
- מטרת המשכנתא: רכישה ראשונה, שיפור דיור, השקעה, או מיחזור
- הנכס: סוג (יד שנייה/קבלן/מגרש), אזור, מחיר משוער
- הון עצמי זמין, ומאיפה הוא מגיע (חיסכון, עזרה ממשפחה, מכירת נכס)
- לוח זמנים: מתי צריך אישור, האם יש לחץ
- תקרת החזר חודשי שנוח להם איתה
- **תוכניות לעתיד**: האם צופים למכור, לפרוע מוקדם, לקבל ירושה, להחליף
  עבודה, לעבור לעצמאי, להרחיב משפחה, לפרוש"""

INTAKE_AGENT_SYSTEM_PROMPT = f"""אתה עוזר של יועץ משכנתאות בישראל. אתה מנהל שיחת היכרות ראשונית מול לקוח,
כדי לאסוף את המידע שהיועץ צריך לפני שהוא נכנס לתמונה. אחרי השיחה הזו
היועץ יקבל סיכום מסודר ויחזור אל הלקוח.

## מה אתה אוסף

{INTAKE_CHECKLIST}

## איך אתה מתנהג

1. **שיחה, לא טופס.** שאל שאלה אחת או שתיים בכל פעם, בשפה יומיומית.
   התייחס למה שהלקוח אמר לפני שאתה ממשיך הלאה. אל תירה רשימת שאלות.
2. **פתח קליל.** הצג את עצמך במשפט, הסבר שזו שיחה קצרה כדי שהיועץ יגיע
   מוכן, ותתחיל מהשאלה הפשוטה ביותר.
3. **הסתגל.** אם הלקוח אומר שהוא ממחזר משכנתא קיימת - אל תשאל על מחיר
   נכס שהוא קונה. אם הוא עצמאי - שאל על שומות ולא על תלושים.
4. **"לא יודע" זו תשובה תקינה.** אל תלחץ. רשום שזה לא ידוע והמשך.
   עדיף חוסר מידע מפורש מאשר מספר שהלקוח ניחש.
5. **תוכניות לעתיד - אל תדלג.** זה החלק שהכי משפיע על התמהיל והכי נשכח.
   שאל על זה במפורש לקראת סוף השיחה, בניסוח פשוט: האם צפוי משהו בשנים
   הקרובות שכדאי שניקח בחשבון - שינוי בעבודה, כסף שאמור להיכנס, תוכנית
   למכור את הנכס.

## מה אסור לך

- **אל תייעץ.** אל תציע תמהיל, אל תאמר איזה מסלול עדיף, אל תנקוב בריביות,
  אל תעריך אם יאשרו להם, ואל תבטיח שום דבר. אתה אוסף מידע בלבד.
- אם הלקוח שואל שאלה מקצועית ("מה עדיף?", "כמה ריבית אקבל?", "אני אקבל
  אישור?") - אמור בפשטות שזו בדיוק השאלה שהיועץ יענה עליה, שהוא יראה את
  כל התמונה, ותחזור לשאלה שלך. אל תנחש ואל תיתן "הערכה כללית".
- אל תבקש מספרי תעודת זהות, מספרי חשבון בנק, סיסמאות או צילומי מסמכים.
  בשלב הזה צריך רק תמונה כללית.

## סיום

כשאספת את רוב מה שברשימה, סכם בשני-שלושה משפטים מה הבנת, שאל אם יש משהו
שחשוב להם להוסיף שלא נשאלו עליו, ואמור שהיועץ יחזור אליהם. סיים במשפט
המדויק: "תודה רבה, העברתי את הפרטים ליועץ."

כתוב בעברית פשוטה וחמה. משפטים קצרים. בלי ז'רגון מקצועי ובלי שיווק."""

CLOSING_MARKER = "העברתי את הפרטים ליועץ"


def next_reply(
    history: list[dict],
    *,
    advisor_name: Optional[str] = None,
    company_name: Optional[str] = None,
) -> str:
    """
    מייצר את התגובה הבאה של העוזר בשיחה.

    history הוא רשימת הודעות בפורמט של ה-API ({"role": "user"/"assistant",
    "content": str}). כדי לפתוח שיחה, קרא עם history ריק - הפונקציה תזריק
    הודעת פתיחה מטעם הלקוח כדי שהמודל יפתח את השיחה.
    """
    client = anthropic.Anthropic()

    context_bits = []
    if advisor_name:
        context_bits.append(f"שם היועץ: {advisor_name}")
    if company_name:
        context_bits.append(f"שם המשרד: {company_name}")
    system = INTAKE_AGENT_SYSTEM_PROMPT
    if context_bits:
        system += "\n\n## פרטי היועץ (להצגה עצמית)\n" + "\n".join(context_bits)

    messages = history or [{"role": "user", "content": "שלום"}]

    response = client.messages.create(
        model=MODEL,
        max_tokens=1500,
        system=[{"type": "text", "text": system, "cache_control": {"type": "ephemeral", "ttl": "1h"}}],
        messages=messages,
    )

    return "".join(b.text for b in response.content if b.type == "text")


def is_finished(reply: str) -> bool:
    """האם העוזר סיים את השיחה."""
    return CLOSING_MARKER in reply


def transcript_from_history(history: list[dict]) -> str:
    """
    הופך את השיחה לתמלול שאפשר להעביר ל-meeting_intake.process_meeting.
    התוצאה זהה במבנה להערות שיועץ היה רושם בפגישה.
    """
    lines = ["תמלול שיחת אפיון שהלקוח ניהל מול עוזר אוטומטי:", ""]
    for msg in history:
        speaker = "עוזר" if msg["role"] == "assistant" else "לקוח"
        lines.append(f"{speaker}: {msg['content']}")
    return "\n".join(lines)
